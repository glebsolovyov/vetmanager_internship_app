import 'package:flutter/material.dart';

/// {@template visit_screen}
/// VisitScreen widget.
/// {@endtemplate}
class VisitScreen extends StatelessWidget {
  /// {@macro visit_screen}
  const VisitScreen({
    super.key, // ignore: unused_element
  });

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('VisitScreen'));
}
